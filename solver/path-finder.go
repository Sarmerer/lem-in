package solver

import "lem-in/types"

func Solve(data *types.Data) {
	path := []string{"0", "2", "3", "1"}

	for data.AntsAmount != 0 {
		
		data.AntsAmount--
	}
	// for _, d := range data.Rooms {
	// 	for _, conn := range d.Connections {

	// 	}
	// }
}
